import 'package:flutter/material.dart';

class ScreenShort extends StatelessWidget {
  const ScreenShort({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('ScreenShort')));
  }
}
